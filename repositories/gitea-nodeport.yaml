apiVersion: v1
kind: Service
metadata:
  name: gitea-ssh
  namespace: repositories
  labels:
    app: gitea-ssh
spec:
  ports:
  - name: ssh
    port: 22
    targetPort: 22
    nodePort: 32002
  selector:
    app: gitea
  type: NodePort
